DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-seguro"

# Correo SMTP (misma convencion que SAA2). Al registrar un aprendiz se envia su QR al correo indicado.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="tu-correo@gmail.com"
SMTP_PASS="tu-contrasena-de-aplicacion"

# URL publica de la app (para enlaces de recuperacion de contraseña)
APP_URL="http://localhost:3000"
